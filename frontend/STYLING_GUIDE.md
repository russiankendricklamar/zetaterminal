# Apple Liquid Glass Styling Guide

## Обзор
Проект использует единый стиль Apple Liquid Glass для всех страниц. Все стили определены в `src/assets/styles/main.css` и доступны через универсальные классы.

## Универсальные классы

### Карточки и панели
```html
<!-- Базовая карточка -->
<div class="glass-card">
  <!-- содержимое -->
</div>

<!-- Большая панель -->
<div class="glass-panel">
  <!-- содержимое -->
</div>
```

### Кнопки
```html
<!-- Стеклянная кнопка -->
<button class="btn-glass">Текст</button>

<!-- Primary кнопка -->
<button class="btn-glass primary">Текст</button>
```

### Инпуты
```html
<input type="text" class="glass-input" placeholder="Введите текст">
```

### Бейджи
```html
<span class="glass-pill">Текст</span>
```

### Таблицы
```html
<table class="glass-table">
  <thead>
    <tr>
      <th>Заголовок</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Данные</td>
    </tr>
  </tbody>
</table>
```

### Контейнеры страниц
```html
<div class="page-container">
  <!-- содержимое страницы -->
</div>
```

### Заголовки секций
```html
<div class="section-header">
  <div>
    <h1 class="section-title">Заголовок</h1>
    <p class="section-subtitle">Подзаголовок</p>
  </div>
</div>
```

### Заголовки карточек
```html
<div class="card-header">
  <h3>Заголовок карточки</h3>
  <span class="card-subtitle">Подзаголовок</span>
</div>
```

### Сетки
```html
<!-- 2 колонки -->
<div class="grid-2">
  <div>Элемент 1</div>
  <div>Элемент 2</div>
</div>

<!-- 3 колонки -->
<div class="grid-3">
  <div>Элемент 1</div>
  <div>Элемент 2</div>
  <div>Элемент 3</div>
</div>

<!-- 4 колонки -->
<div class="grid-4">
  <div>Элемент 1</div>
  <div>Элемент 2</div>
  <div>Элемент 3</div>
  <div>Элемент 4</div>
</div>

<!-- Полная ширина -->
<div class="full-width">
  <!-- растягивается на всю ширину сетки -->
</div>
```

## CSS переменные

Все переменные определены в `:root`:

```css
--glass-tint: rgba(30, 32, 40, 0.40);
--glass-border: rgba(255, 255, 255, 0.08);
--glass-blur: 40px;
--glass-sat: 180%;
--text-primary: rgba(255, 255, 255, 0.95);
--text-secondary: rgba(255, 255, 255, 0.60);
--text-tertiary: rgba(255, 255, 255, 0.40);
--radius-sm: 8px;
--radius-md: 14px;
--radius-lg: 20px;
```

## Миграция существующих страниц

### Шаг 1: Замените локальные стили карточек
**Было:**
```css
.my-card {
  background: rgba(30,32,40,0.4);
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,0.08);
}
```

**Стало:**
```html
<div class="glass-card">
  <!-- или используйте класс .card, если он уже определен -->
</div>
```

### Шаг 2: Обновите кнопки
**Было:**
```css
.btn-custom {
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.2);
}
```

**Стало:**
```html
<button class="btn-glass">Текст</button>
```

### Шаг 3: Добавьте backdrop-filter к элементам
Все элементы с полупрозрачным фоном должны иметь:
```css
backdrop-filter: blur(40px) saturate(180%);
-webkit-backdrop-filter: blur(40px) saturate(180%);
```

### Шаг 4: Используйте единые радиусы
- Маленькие элементы: `var(--radius-sm)` (8px)
- Средние элементы: `var(--radius-md)` (14px)
- Большие элементы: `var(--radius-lg)` (20px)

## Примеры обновленных страниц

- ✅ `VolatilitySurface.vue` - полностью обновлена
- 🔄 Остальные страницы - в процессе миграции

## Важные замечания

1. **Всегда используйте backdrop-filter** для элементов с полупрозрачным фоном
2. **Используйте единые переменные** из `:root` для цветов и радиусов
3. **Добавляйте transition** для плавных анимаций: `transition: all 0.3s cubic-bezier(0.2, 0.8, 0.2, 1)`
4. **Используйте inset тени** для эффекта глубины: `inset 0 1px 0 rgba(255, 255, 255, 0.1)`
